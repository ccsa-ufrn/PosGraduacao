/* 
This is just a model for the real json stored at '../env' directory.

- Do not write any comments (like this one) in '../env/api_keys.json', so
keep only the neat json.
- Caution: do not include the real directory in version control,
so keep 'env' gitignore.
- Despite this security measure, the real keys should only work 
work for a certain IP.
*/

{
    "google_maps_js_api": {
        "key": "secret"
    }
}